/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 't3.ftcdn.net', 'i.pinimg.com', 'images.pexels.com', 'img.freepik.com', 'example.com']
  },
}

module.exports = nextConfig
